TODO:
- keyboard configuration
- default gamepad mapping*
	+ change icons
	+ if nintendo controller -> change Input.gamepadMapper to Nintendo buttons
		. Only 'ok' and 'cancel' need swapping
	+ if nintendo controller -> function to update Input.handlers 
		. Only 'ok' and 'cancel' need swapping
- load keyboard/gamepad mapping
	+ function to update Input.handlers
		. Loop through array and replace item[2]

* = pseudocode below for detecting controller
if vendor = 057e || nintendo || pro || switch
	give NINTENDO mapping, give SWITCH icons
else 
	give STANDARD mapping
	if vendor = 054c || playstation || sony || PS || PS3 || PS4 || dualshock
		give PS icons
	else if vendor = 045e || xbox || x-box || 360 || microsoft
		give XBOX icons
	else
		give GENERIC icons